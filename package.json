{
  "name": "@metaframer/kernel",
  "version": "0.0.0-planning",
  "private": true,
  "description": "Planning-only repository reservation for the MetaFramer runtime kernel boundary.",
  "type": "module",
  "scripts": {
    "check": "node tools/check-repository-boundary.mjs && node tools/check-control-plane-bootstrap.mjs",
    "check:sources": "node tools/check-source-snapshot.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
